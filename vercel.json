{
  "version": 2,
   "builds": [
        {
           "src": "/(.*)",
            "use": "@vercel/node"
        }
    ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/",
      "methods": ["GET", "PUT", "PATCH", "DELETE", "POST", "OPTIONS"]
    }
  ]
}
